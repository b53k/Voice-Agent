services:
  voice-bot:
    build: .
    container_name: voice-bot
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./analysis:/app/analysis
    stdin_open: true
    tty: true
    command: bash